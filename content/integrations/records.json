{
  "id": "records",
  "name": "Records",
  "version": "1.0.0",
  "description": "Personal knowledge store — notes, decisions, research, bookmarks managed by AI agents",
  "icon": "file-text",
  "profile": "records",
  "personalDefault": true,
  "mcp": {
    "command": "npx",
    "args": ["-y", "@humanagencyp/records-mcp@latest"]
  },
  "credentials": {
    "fields": [
      { "key": "databaseUrl", "label": "Database URL (optional)", "type": "text", "placeholder": "Leave empty for local SQLite", "optional": true }
    ],
    "envMapping": { "DATABASE_URL": "databaseUrl" }
  },
  "oauth": null,
  "toolGating": {
    "default": {
      "executionMapping": {},
      "staticExecution": {}
    },
    "overrides": {
      "search_records": { "category": "read" },
      "get_record": { "category": "read" },
      "list_records": { "category": "read" },
      "export_records": { "category": "read" },
      "create_record": { "executionMapping": {}, "staticExecution": {} },
      "update_record": { "executionMapping": {}, "staticExecution": {} },
      "delete_record": { "executionMapping": {}, "staticExecution": {} },
      "archive_record": { "executionMapping": {}, "staticExecution": {} }
    }
  },
  "setupHint": "No configuration needed — works out of the box.",
  "setupGuide": [
    {
      "title": "Ready to use",
      "description": "Records stores data locally on your machine. No external account or API key needed. Your AI agents can create notes, decisions, research, and bookmarks."
    },
    {
      "title": "Optional: use a shared database",
      "description": "For team use, provide a PostgreSQL connection string in the Database URL field. Otherwise, leave it empty to use local SQLite storage."
    }
  ]
}
